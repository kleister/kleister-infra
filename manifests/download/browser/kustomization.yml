---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kleister-system

resources:
  - networkpolicy.yml
  - ../../../../../kustomhippie/s3browser # github.com/kustomhippie/s3browser?ref=master

patchesStrategicMerge:
  - deployment.yml

generators:
  - ksops.yml

configMapGenerator:
  - name: s3browser-server
    behavior: merge
    literals:
      - S3_SITENAME=kleister
      - S3_ENDPOINT=minio.kleister.tech
      - S3_BUCKET=uploads

...
